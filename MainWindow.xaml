<Window x:Class="videoCut.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:videoCut.Views"
        mc:Ignorable="d"
        Title="视频下载与切片工具"
        Icon="/assets/icon/app.ico"
        Width="1120"
        Height="760"
        MinWidth="980"
        MinHeight="760"
        MaxHeight="760"
        ResizeMode="CanMinimize"
        WindowStartupLocation="CenterScreen">
    <Grid Background="{StaticResource AppBackgroundBrush}">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="视频下载与切片工具"
                               FontSize="26"
                               FontWeight="Bold" />
                    <TextBlock Margin="0,6,0,0"
                               Foreground="{StaticResource SecondaryTextBrush}"
                               Text="先下载视频，再进入切片页面完成时间范围配置与导出。" />
                </StackPanel>

                <Border Grid.Column="1"
                        VerticalAlignment="Bottom"
                        Background="{StaticResource SurfaceBrush}"
                        BorderBrush="{StaticResource LineBrush}"
                        BorderThickness="1"
                        CornerRadius="10"
                        ClipToBounds="True">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="1" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Button Grid.Column="0"
                                Content="视频下载"
                                Command="{Binding ShowDownloadPageCommand}">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource SegmentedNavLeftButtonStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsDownloadPageActive}" Value="True">
                                            <Setter Property="Background" Value="{StaticResource PrimaryBrush}" />
                                            <Setter Property="Foreground" Value="White" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>

                        <Border Grid.Column="1"
                                Background="{StaticResource LineBrush}" />

                        <Button Grid.Column="2"
                                Content="视频切片"
                                Command="{Binding ShowCutPageCommand}">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource SegmentedNavRightButtonStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsCutPageActive}" Value="True">
                                            <Setter Property="Background" Value="{StaticResource PrimaryBrush}" />
                                            <Setter Property="Foreground" Value="White" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </Grid>
                </Border>
            </Grid>

            <Border Grid.Row="1"
                    BorderBrush="{StaticResource LineBrush}"
                    BorderThickness="0,1,0,0"
                    Margin="0,0,0,16" />

            <Grid Grid.Row="2">
                <views:DownloadPage>
                    <views:DownloadPage.Style>
                        <Style TargetType="views:DownloadPage">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsDownloadPageActive}" Value="True">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </views:DownloadPage.Style>
                </views:DownloadPage>

                <views:CutPage>
                    <views:CutPage.Style>
                        <Style TargetType="views:CutPage">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsCutPageActive}" Value="True">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </views:CutPage.Style>
                </views:CutPage>
            </Grid>
        </Grid>
    </Grid>
</Window>
